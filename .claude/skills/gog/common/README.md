# gog CLI — 共通設定

## 環境セットアップ

gog CLI をインストール後、環境に合わせてパスを設定してください。

## グローバルフラグ

- `--json` — JSON出力（スクリプト向け）
- `--plain` — TSV出力（パース向け）
- `--force` — 確認スキップ
- `--no-input` — プロンプトなし（CI向け）

## 注意事項

- `--help` を使ってサブコマンドの正確な構文を確認してから実行すること
- フラグ名と位置引数を混同しないこと（例: `--list` ではなく位置引数 `<tasklistId>`）
